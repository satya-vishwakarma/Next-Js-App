export enum STATUS {
  ACTIVE = 1,
  DELETE = 0,
  INACTIVE = 2,
}

export enum ROLES {
  ADMIN = 1,
  STUDENT = 2,
  TEACHER = 3,
  GUEST = 4,
}

export enum GENDER {
  MALE = 'male',
  FEMALE = 'female',
}
